version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: iimst-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
      MONGO_INITDB_DATABASE: iimst
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
